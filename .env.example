# Leave blank to use the Vite dev proxy (see vite.config.js), which forwards
# /api and /ws to http://localhost:8000.
#
# For a deployed backend, set the full URL, e.g.:
# VITE_API_BASE_URL=https://api.eapen.yourdomain.com
VITE_API_BASE_URL=
